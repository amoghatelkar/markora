{
  "installCommand": "npm ci && npm ci --prefix website",
  "buildCommand": "npm run build:web && npm run build --prefix website && node scripts/assemble-vercel.mjs",
  "outputDirectory": "website/dist",
  "rewrites": [
    { "source": "/app", "destination": "/app/index.html" }
  ]
}
